export const metadata = {
  title: 'Create Account | DRAVE',
  description: 'Join DRAVE and stay connected to every drop.',
}

export default function SignUpLayout({ children }) {
  return children
}
